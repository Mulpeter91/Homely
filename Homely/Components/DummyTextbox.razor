<!-- attributes to the right override the left, eg placeholder -->
<input type="text" placeholder="default" @attributes="AdditionalParameters" />

@code {
    // This captures arbitary values sent to the component
    [Parameter(CaptureUnmatchedValues =true)]
    public IDictionary<string, object> AdditionalParameters { get; set; }
}
