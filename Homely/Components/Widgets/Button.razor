<button type="button" class="btn @type" @onclick="onClick">@text</button>

@code {
    [Parameter] public string text { get; set; } = "Button";
    [Parameter] public string type { get; set; } = "btn-primary";
    [Parameter] public EventCallback onClick { get; set; }
}
